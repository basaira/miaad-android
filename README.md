# Miaad Android

Cloud-build repository for the Miaad Android app (`com.miaad.app`).

The Android source bundle is built automatically by GitHub Actions and the generated debug APK is published as a workflow artifact.
